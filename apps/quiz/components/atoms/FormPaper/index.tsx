import styled from 'styled-components';
import Paper, { PaperProps } from '@mui/material/Paper';

const FormPaper = styled(Paper)<PaperProps>`
  height: 680px;
  margin-top: 12px;
  border-radius: 12px;
`;

export default FormPaper